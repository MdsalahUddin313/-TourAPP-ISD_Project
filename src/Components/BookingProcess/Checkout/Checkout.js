import React from 'react';
import {SendCartData} from '../HotelsSelection/HotelsLists';
import {useContext} from 'react';



const Checkout = (body) => {
 //   const context = useContext(SendCartData);
      

    return (
        <div>
          {console.log(body)}
        </div>
    );
};

export default Checkout;